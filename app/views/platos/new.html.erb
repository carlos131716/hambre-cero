<div class="container mx-auto mt-8">
  <div class="flex justify-center">
    <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-6">
      <h2 class="text-2xl font-bold text-center text-teal-600 mb-6">Agregar Nuevo Plato</h2>
      
      <%= form_with model: @plato, url: platos_path, local: true, class: "space-y-4" do |form| %>
        <!-- Campo Nombre -->
        <div>
          <%= form.label :name_comida, "Nombre del Plato", class: "block text-gray-700 font-semibold mb-2" %>
          <%= form.text_field :name_comida, class: "w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500 focus:border-teal-500" %>
        </div>
      
        <!-- Botón Guardar -->
        <div class="flex justify-center">
          <%= form.submit "Guardar", class: "bg-green-500 text-white px-6 py-2 rounded-lg shadow-md hover:bg-green-600 transition duration-300" %>
        </div>
      <% end %>
      
    </div>
  </div>
</div>
